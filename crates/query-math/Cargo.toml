[package]
name = "query-math"
version = "0.1.0"
edition = "2021"
description = "Experimental Rust/WASM cost-model primitives for SQL Visual Optimizer"
license = "MIT"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
wasm-bindgen = "0.2"
